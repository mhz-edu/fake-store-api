version: '3.1'

services:
  app:
    build: .
    env_file:
      - .test.env
    ports:
      - '3000:3000'
    depends_on:
      - mongodb
  mongodb: 
    image: mongo
    restart: always
    # volumes:
    #   - ./mongo_data:/data/db
    env_file:
      - .test.env

  # db:
  #   image: postgres:14
  #   restart: always
  #   env_file:
  #     - .test.env
  #   ports:
  #     - '5432:5432'